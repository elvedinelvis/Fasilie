package se.fasilie.core.constants

import se.fasilie.core.BuildConfig

object Constants {
    val API_URL = BuildConfig.BASE_URL

    object DatabaseConstants {
        val DB_NAME = "db_fasilie"
    }
}
